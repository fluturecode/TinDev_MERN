import React, { useState } from "react";

function Card() {
	const [people, setPeople] = useState([]);

	return (
		<div className="card">
			<h1>tinder cards</h1>
		</div>
	);
}

export default Card;
